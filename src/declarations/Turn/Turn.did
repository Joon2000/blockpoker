service : {
  Fold: (text) -> ();
  getGameData: (text) -> (nat, nat, text, bool, nat, nat, nat, text, 
   text, nat, nat);
  getPlayerCards: (text) -> (opt record {
                                   opt nat;
                                   opt nat;
                                   opt nat;
                                 }) query;
  initializeCards: () -> (opt null);
  playerReady: (text) -> (text);
}
